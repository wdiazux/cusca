// FitVids
// -------

import fitVids = require('fitvids');

const $ = $;
$(function () {
    // Start fitVids
    var postContent = $('#site-main');
    postContent.fitVids();
    // End fitVids
});
